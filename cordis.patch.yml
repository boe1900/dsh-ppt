# DSH 0.1.7 host-plane registration. Session tools and the Skill are mounted
# by presets/ppt.patch.yml through the native agent-preset mechanism.
- insert:
    - id: dsh-ppt
      name: '@cola1900/dsh-ppt'
      config:
        root: !!js dshHomePath('kimi-ppt')
